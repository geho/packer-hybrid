## Tasks

1. - [x] Describe required header template (hashbang, SPDX ID, copyright, timestamp, spec/diagram references) in `specs/governance/spec.md`.
2. - [x] Define scope (which files must include the header) and ordering rules (newline spacing, comment prefix).
3. - [x] Outline automation/testing expectations (lint hook, CI script) to enforce headers.
4. - [x] Run `openspec validate add-license-header-policy --strict`.
