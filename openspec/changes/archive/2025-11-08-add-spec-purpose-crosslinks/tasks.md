## Tasks

1. - [x] Replace the Purpose placeholder text in every spec under `openspec/specs/` with accurate summaries.
2. - [x] Update `specs/hybridcore/spec.md` to restate the module hierarchy guidance and reference each module spec.
3. - [x] Add explicit cross-links in each hybridcore module spec back to the umbrella spec (and cite their role).
4. - [x] Run `openspec validate add-spec-purpose-crosslinks --strict`.
