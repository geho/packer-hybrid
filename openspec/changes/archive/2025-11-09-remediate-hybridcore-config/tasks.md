## Tasks

1. - [ ] Add an OS image variant/platform overlays requirement (with precedence examples) to `specs/hybridcore-config/spec.md`.
2. - [ ] Extend the extendability checklist with secret-handling/redaction/test requirements.
3. - [ ] Reference templates/state Open Issues so config spec stays aligned (cross-links).
4. - [ ] Document manifest/checksum integration with `state/config/<env>.json` and change-detection rules.
5. - [ ] Update `docs/spec-remediations/hybridcore-config-remediations.md` (move addressed topics to “Closed Topics”), then run `openspec validate remediate-hybridcore-config --strict`.
