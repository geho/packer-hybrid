# Tasks

- [ ] Verify no concurrent change is editing Puppet mode metadata instructions.
- [ ] Update `docs/drafts/puppet-agent-modes.md` to match the spec’s storage path and reference files.
- [ ] Run `openspec validate align-puppet-mode-doc --strict` after drafting deltas.
- [ ] Review with provisioning spec owner/documentation maintainer.
