# Tasks

- [ ] Confirm no other proposals already touch the opt-in doc or fallback requirement.
- [ ] Update `docs/drafts/ansible-puppet-opt-in.md` with the spec-accurate fallback description.
- [ ] Run `openspec validate align-ansible-puppet-opt-in-doc --strict` (after spec/doc deltas) and fix issues.
- [ ] Circulate with provisioning spec owner for review.
