# linter: shellcheck

## commands

### version

```shell
shellcheck --version
```

### help

```shell
shellcheck --help
```

### linting

```shell
shellcheck [options] file.sh
```

## options (common)

- `--format=gcc` – make output diff/IDE-friendly.
- `--shell=bash|sh|ksh` – override detected shell when needed.
