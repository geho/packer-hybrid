# Hybridcore Logs Remediations

## Open Topics

1. _None – repopulate after the next assessment if additional log gaps surface._

## Closed Topics

1. **Integrity** – Sink extensions now document syslog/cloud guidance plus formatter parity tests (`remediate-hybridcore-logs-2025-11`).
2. **Completeness / Alignment** – Retention mapping table ties CLI flags/env vars to security policy (`remediate-hybridcore-logs-2025-11`).
3. **Consistency** – Diagnostics integration requirement documents log scrub hook before `diag` runs (`remediate-hybridcore-logs-2025-11`).
4. **Ambiguities** – Context schema table defines required/optional keys and defaults (`remediate-hybridcore-logs-2025-11`).
5. **Alignment** – Logging diagrams live in `specs/hybridcore-logs/` and docs link back per governance (`remediate-hybridcore-logs-2025-11`).
6. **Log shipping & flags** – Spec now covers sink extensions and CLI flag interplay.
7. **Retention guidance** – Added retention/audit alignment requirement.
