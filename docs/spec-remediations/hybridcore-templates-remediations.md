# Hybridcore Templates Remediations

## Open Topics

1. _None – new gaps will be recorded during the next assessment._

## Closed Topics

1. **Variant taxonomy** – `openspec/specs/hybridcore-templates/spec.md#requirement-variant-naming--layout` plus the [variant taxonomy diagram](openspec/specs/hybridcore-templates/variant-taxonomy.md) document canonical naming (`remediate-hybridcore-templates-2025-11`).
2. **Checksum cache & invalidation** – `#requirement-checksum-cache--invalidation` covers `state/templates/cache/` storage, CLI parity, and CI coverage (`remediate-hybridcore-templates-2025-11`).
3. **State sync contract** – `#requirement-builder-manifest-state-sync` and the [state sync flow](openspec/specs/hybridcore-templates/state-sync-flow.md) explain manifest ↔ state behavior (`remediate-hybridcore-templates-2025-11`).
4. **Provisioner alignment** – `#requirement-provisioner-metadata-alignment` references provisioning specs and adds drift validation between templates/provisioners (`remediate-hybridcore-templates-2025-11`).
5. **Diagram governance** – `#requirement-diagram-governance` ensures onboarding/state sync diagrams live in the spec directory and docs link back (`remediate-hybridcore-templates-2025-11`).
6. **Metadata schema completeness** – Addressed via the Template Metadata & Change Detection requirement.
