# Hybridcore Config Remediations

## Open Topics

1. _None – populated at the next assessment if new gaps appear._

## Closed Topics

1. **Alignment** – Config spec references templates/state remediation drafts (`remediate-hybridcore-config-2025-11`).
2. **Provenance metadata** – Serialization requirement documents manifests + secret indirection metadata (`remediate-hybridcore-config-2025-11`).
3. **Pipeline diagram** – Added `specs/hybridcore-config/config-integrations.md` and linked docs (`remediate-hybridcore-config-2025-11`).
4. **Schema severity mapping** – Spec defines severity table/exit codes with tests (`remediate-hybridcore-config-2025-11`).
5. **Provisioning references** – Added config→provisioning mapping requirement/table referencing provisioning spec/templates (`remediate-hybridcore-config-2025-11`).
6. **Variants/Overlays** – OS image variant + platform overlay precedence documented.
7. **Secrets** – Extendability checklist now covers secret redaction/tests.
8. **Manifest/State** – Config spec now describes manifest/checksum integration with state and drift detection.
